---
'@kotaio/adaptive-form': major
---

Rename DynamicForm to AdaptiveForm. This is a breaking change — the `DynamicForm` export has been removed and replaced with `AdaptiveForm`. The `AdaptiveFormProps` type is now also exported.
