---
"@kotaio/adaptive-requirements-engine": minor
"@kotaio/adaptive-form": minor
---

Rename packages from `@kota` to `@kotaio` scope and rename `dynamic-form` to `adaptive-form` for npm publishing under the `@kotaio` org.
