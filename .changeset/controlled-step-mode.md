---
'@kotaio/adaptive-form': minor
---

Add controlled step mode to AdaptiveFormProvider via `currentStepId`, `onStepChange`, and `defaultStepId` props, enabling consumers to sync step state with external sources like URL search params.
