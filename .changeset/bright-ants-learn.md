---
'@kotaio/adaptive-requirements-engine': patch
'@kotaio/adaptive-form': patch
---

Apply schema field `defaultValue`s in the engine and use them to auto-initialize uncontrolled React forms when no form-level default is provided.
